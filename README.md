# Portfolio — Ghulam Fareed

This is a simple static portfolio website generated from your resume content.

How to preview locally

1. Open `d:\Portfoli\index.html` in your web browser (double-click or right-click -> Open).

Optional: Deploy to GitHub Pages

1. Create a GitHub repository and push the `index.html`, `styles.css`, and `script.js` files.
2. In the repository settings enable GitHub Pages from the `main` branch (root). The site will be available at `https://<your-username>.github.io/<repo>`.

If you want, I can:
- tweak the visual design
- add a downloadable CV link or image assets
- integrate a contact backend or Netlify form

Tell me which changes you'd like next.
"# portfolio" 
